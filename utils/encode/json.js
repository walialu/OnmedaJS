/**
 * Encodes any given valid ISO string to UTF-8.
 * @function
 * @param {String} str A valid ISO string.
 * @return {String} UTF-8 encoded string.
 * @static
 * @example var isoStr = 'mÃ¼sli';
 * console.log( onmeda.utils.encode.utf8_encode( isoStr ) );
 */
window.onmeda.utils.encode.json = function ( str ) {
	// @TODO: TBD onmeda.utils.encode.json( str )
}
